# iCore - ElementFlow (No Database Version)

This version does NOT use SQLite database.

It only shows success messages after form submission.

## Run locally

pip install -r requirements.txt
streamlit run app.py

## Deploy on Streamlit Cloud

1. Upload files to GitHub
2. Deploy using Streamlit Community Cloud
